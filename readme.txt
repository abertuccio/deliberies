deszipear el archivo
cd delibery

con nodejs instalado "npm install http-server -g" 

luego "http-server ./build"  (el puerto por defecto es el 8080, si estuviera ocupado, con la opcion -p se puede especificar otro puerto Ej: http-server ./build -p 3500)

Abrir el browser en http://localhost:8080/ (o el puerto que se especificó)



